package main

import "fmt"

func main() {
	//slices are  dynamic arrays
	var s []string
	fmt.Println("uninit:", s, s == nil, len(s) == 0)

	s = make([]string, 3)
	fmt.Println("emp:", s, "len:", len(s), "cap:", cap(s))

	s[0] = "a"
	fmt.Println(s)
}
